services:
  - type: web
    name: philocontext
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd server && uvicorn api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false
